/*
    Default Export:
     In this case, if import occurs by any name, Suppose XYZ, then the default value will be exported. It is generally used to export a single object, function, or variable.
*/

const a = "Harry";
const b = "Rohan";
const c = "Aakash";
const d = "Priyanka";

export default b; // default export
